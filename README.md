# congenial-fiesta

## Up and running

### add your `.env` file based on `.env.example`

`yarn` or `yarn install`

`docker-compose up -d`

`yarn dev`

## Running tests
`yarn test` or `yarn test:watch`
